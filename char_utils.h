#ifndef CHAR_UTILS_H
#define CHAR_UTILS_H

// Character utility function prototypes will go here

int is_alpha(char c);
int is_digit(char c);
int is_alnum(char c);
int is_space(char c);
int to_upper(char c);
int to_lower(char c);

#endif // CHAR_UTILS_H
